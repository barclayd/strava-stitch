// Stable public entry URLs; the deployment asset build bundles their dependencies.
export const entryHref = '/client/entry.js'
export const entryPreloads = [
  '/client/workspace.js',
  '/client/route-map.js',
  '/client/upload-form.js',
]
