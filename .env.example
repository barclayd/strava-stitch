APP_ORIGIN=http://localhost:44100
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
SESSION_SECRET=
TOKEN_ENCRYPTION_KEY=
# Optional until deployed and a webhook subscription has been registered:
STRAVA_WEBHOOK_VERIFY_TOKEN=
STRAVA_WEBHOOK_SUBSCRIPTION_ID=
# Enable only behind a trusted TLS proxy that overwrites forwarding headers:
TRUST_PROXY=false
# Optional overrides for the listen address and persistent storage:
# HOST=127.0.0.1
# PORT=44100
# DATABASE_PATH=./db/stitch.sqlite
# SESSION_DIR=./tmp/sessions
